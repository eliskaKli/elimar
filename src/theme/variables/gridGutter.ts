export interface GridGutter {
    xs: number
    sm: number
    md: number
    lg: number
}

const gridGutter: GridGutter = {
    xs: 8,
    sm: 16,
    md: 32,
    lg: 64,
}

export default gridGutter