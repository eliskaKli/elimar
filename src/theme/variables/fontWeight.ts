export interface FontWeight {
    bold: number
    light: number
    medium: number
    normal: number
    semibold: number
}

const fontWeight: FontWeight = {
    bold: 700,
    light: 300,
    medium: 500,
    normal: 400,
    semibold: 600,
}

export default fontWeight